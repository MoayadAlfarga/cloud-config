spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3307/accounts
    password: MySql20
  application:
    name: accounts
  liquibase:
    enabled: 'true'
    change-log: classpath:db.changelog/db.changelog-master.xml
  jpa:
    hibernate:
      ddl-auto: none
server:
  servlet:
    context-path: /account-service
build:
  version: "1.0"

accounts:
  message: "Welcome to the Accounts Service. This is the Production Environment "
  contactDetails:
    name: "Reine    - Product Owner"
    email: "aishwarya@eazybank.com"
  onCallSupport:
    - (453) 392-4829
    - (236) 203-0384
